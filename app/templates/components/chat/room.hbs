<div class="chat-room flex items-center p-3 rounded-lg cursor-pointer mb-2" data-room-id="{{room.id}}">
    <div class="chat-room-avatar mr-3">
        <img src="{{room.avatar}}" alt="{{room.name}}" class="w-10 h-10 rounded-full object-cover">
    </div>
    <div class="chat-room-name font-medium text-gray-700">
        {{room.name}}
    </div>
</div>