<div class="chat-user flex items-center p-3 rounded-lg cursor-pointer mb-2">
    <div class="chat-user-avatar mr-3">
        <img src="{{user.avatar}}" alt="{{user.name}}" class="w-10 h-10 rounded-full object-cover">
    </div>
    <div class="chat-user-name font-medium text-gray-700">
        {{user.name}}
    </div>
</div>