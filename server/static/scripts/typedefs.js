/**
 * @typedef {Object.<string, RouteHandler>} Routes
 * @description An object where keys are route names and values are route handler functions.
 */

/**
 * @typedef {Function} RouteHandler
 * @description A function that handles a specific route.
 * @returns {void}
 */
